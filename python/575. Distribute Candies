

class Solution(object):
    def distributeCandies(self, candies):
        """
        :type candies: List[int]
        :rtype: int
        """
        
        unique = len(set(candies))
        length = len(candies)

        if unique > length/2:
            return (length/2)
        else:
            return (unique)

#runtime beats 66% of python submissions
